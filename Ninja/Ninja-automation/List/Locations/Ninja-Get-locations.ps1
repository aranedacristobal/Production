"------------------------------------------------------------------------------------------------------------------------------------------------"
"------------------------------------------------------------------------------------------------------------------------------------------------"

# Variabel
$url = "YOUR URL FOR GET LOCATION"

# Definiera huvudet
$headers = @{
    "Accept"        = "application/json"
    "Authorization" = "Bearer $accessToken"
    "Content-Type"  = "application/json"
}

$response = Invoke-RestMethod -Uri $url -Method Get -Headers $headers

"------------------------------------------------------------------------------------------------------------------------------------------------"
"------------------------------------------------------------------------------------------------------------------------------------------------"

$response | Export-Csv -Path "Ninja\Ninja-automation\List\Locations\locations.csv" -NoTypeInformation